apiVersion: v1
kind: Service
metadata:
  name: bankapp-service
  labels:
    app: bankapp
spec:
  selector:
    app: bankapp
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP