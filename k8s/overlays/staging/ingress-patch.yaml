apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bankapp-ingress
spec:
  rules:
  - host: bankapp-staging.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: bankapp-service
            port:
              number: 80